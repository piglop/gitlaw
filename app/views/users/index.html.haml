- content_for :title, t('.title')

.row-fluid
  .span12
    %table.table.table-striped
      %thead
        %tr
          %th= User.human_attribute_name(:email)
          %th= User.human_attribute_name(:encrypted_password)
          %th= User.human_attribute_name(:reset_password_token)
          %th= User.human_attribute_name(:reset_password_sent_at)
          %th= User.human_attribute_name(:remember_created_at)
          %th= User.human_attribute_name(:sign_in_count)
          %th= User.human_attribute_name(:current_sign_in_at)
          %th= User.human_attribute_name(:last_sign_in_at)
          %th= User.human_attribute_name(:current_sign_in_ip)
          %th= User.human_attribute_name(:last_sign_in_ip)
          %th= t('.actions')
      %tbody
        - @users.each do |user|
          %tr
            %td= user.email
            %td= user.encrypted_password
            %td= user.reset_password_token
            %td= user.reset_password_sent_at
            %td= user.remember_created_at
            %td= user.sign_in_count
            %td= user.current_sign_in_at
            %td= user.last_sign_in_at
            %td= user.current_sign_in_ip
            %td= user.last_sign_in_ip
            %td
              = link_to t('.show'), user_path(user), :class => 'btn btn-mini'
              = link_to t('.edit'), edit_user_path(user), :class => 'btn btn-mini' if can? :update, user
              = link_to t('.destroy'), user_path(user), :method => :delete, :confirm => t('.confirm'), :class => 'btn btn-mini btn-danger' if can? :destroy, user

    = link_to t('.new'), new_user_path, :class => 'btn btn-primary' if can? :create, ::User
